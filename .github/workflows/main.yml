name: News Milvus
on:
  schedule:
    - cron: '*/15 * * * *'  # Runs every 30 minutes
  workflow_dispatch:
    
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: News Milvus 
        run: curl -m 600 ${{ secrets.URL }}
